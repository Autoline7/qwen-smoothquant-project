# Core DL stack - Colab Pro usually has PyTorch 2.x + CUDA 12 preinstalled
torch>=2.1.0
transformers>=4.45.0
accelerate>=0.30.0
datasets>=2.20.0
sentencepiece
protobuf

# Quantization
bitsandbytes>=0.43.0
# Path (b): exporting W8A8 checkpoints loadable by vLLM
llmcompressor>=0.3.0
compressed-tensors>=0.6.0

# Serving (for path b)
vllm>=0.6.0

# Evaluation
evalplus>=0.3.1
bigcodebench>=0.2.5

# Profiling + FLOPs counting
calflops>=0.3.0

# Utilities
tqdm
numpy
pandas
matplotlib
seaborn
